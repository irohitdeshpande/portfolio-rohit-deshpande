---
import Section from "./Section.astro";
import { Image } from "astro:assets";
import type { ProjectProps } from "@types";

interface Props {
  projects: ProjectProps[];
}

const { projects } = Astro.props;
---

<Section text="Projects" href="projects">
  <div class="flex flex-col gap-6 sm:gap-8 lg:gap-10">
    {projects.map(({ name, summary, image, linkPreview, linkSource, date = "", techStack = [] }, index) => (
      <div
        class="group w-full rounded-xl sm:rounded-2xl shadow-lg overflow-hidden transition-transform duration-300 hover:-translate-y-1 hover:shadow-2xl fade-in-up project-card-bg"
        style={`animation-delay: ${index * 80}ms;`}
        data-fade-in-up
      >
        <div class="project-card-inner">
          <h3 class="text-lg sm:text-xl lg:text-2xl font-bold project-title mb-3 sm:mb-4 leading-tight">{name}</h3>
          <div class="flex flex-wrap gap-2 sm:gap-3 mb-4 sm:mb-5 tech-badge-row">
            {techStack.map((tech, i) => (
              <span class="text-xs sm:text-sm lg:text-base tech-badge px-2 sm:px-3 py-1 rounded">{tech}</span>
            ))}
          </div>
          <p class="text-sm sm:text-base lg:text-lg project-description mb-4 sm:mb-6 leading-relaxed max-w-full sm:max-w-3xl lg:max-w-4xl">{summary}</p>
          <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 lg:gap-6 mb-4 sm:mb-6 w-full sm:w-auto project-btn-row">
            <a href={linkSource} target="_blank" class="inline-block rounded-full btn-source text-xs sm:text-sm lg:text-base font-semibold px-4 sm:px-5 lg:px-6 py-2 sm:py-2.5 shadow transition-colors text-center">Source Code</a>
            <a href={linkPreview} target="_blank" class="inline-block rounded-full btn-preview text-xs sm:text-sm lg:text-base font-semibold px-4 sm:px-5 lg:px-6 py-2 sm:py-2.5 shadow transition-colors text-center">Live Preview</a>
          </div>
          {image && (
            <div class="w-full flex justify-center">
              <Image
                class="w-full h-auto object-cover rounded-lg project-image"
                src={image}
                alt={name}
                width="1920"
                height="600"
              />
            </div>
          )}
        </div>
      </div>
    ))}
  </div>
</Section>

<script is:inline>
  if (typeof window !== 'undefined') {
    window.addEventListener('DOMContentLoaded', () => {
      const cards = document.querySelectorAll('[data-fade-in-up]');
      const observer = new window.IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('in-view');
            observer.unobserve(entry.target);
          }
        });
      }, { threshold: 0.15 });
      cards.forEach(card => observer.observe(card));
    });
  }
</script>
<style>
  /* Project card background and border for mobile, matching Education section */
  .project-card-bg {
    background: var(--bg-primary);
    border: 1.5px solid var(--border-color);
    box-shadow: 0 2px 12px var(--shadow);
    padding: 1.25rem 1rem;
    display: flex;
    flex-direction: column;
    align-items: stretch;
    gap: 0.75rem;
  }
  @media (min-width: 640px) {
    .project-card-bg {
      background: linear-gradient(135deg, rgba(255,255,255,0.10) 0%, rgba(255,255,255,0.05) 100%);
      border: none;
      box-shadow: 0 10px 25px var(--shadow);
      padding: 0;
      gap: 0;
    }
  }
  .project-card-inner {
    width: 100%;
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.75rem;
    padding: 0;
  }
  @media (min-width: 640px) {
    .project-card-inner {
      align-items: center;
      padding: 1.5rem 2rem 2rem 2rem;
      gap: 1.25rem;
    }
  }
  .tech-badge-row {
    width: 100%;
    justify-content: flex-start;
  }
  @media (min-width: 640px) {
    .tech-badge-row {
      justify-content: center;
    }
  }
  .project-btn-row {
    width: 100%;
    align-items: flex-start;
  }
  @media (min-width: 640px) {
    .project-btn-row {
      flex-direction: row;
      align-items: center;
      justify-content: center;
    }
  }
  /* Fade-in-up animation for project cards */
  .fade-in-up {
    opacity: 0;
    transform: translateY(32px);
    /* Only animate when .in-view is present */
  }
  .fade-in-up.in-view {
    animation: fadeInUp 0.7s cubic-bezier(0.22, 1, 0.36, 1) forwards;
  }
  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: none;
    }
  }

  /* Project card styling with theme support and mobile optimization */
  /* .project-card background removed; now using Tailwind bg-gradient for consistency with Education */
  /* .project-card:hover background removed; shadow handled by Tailwind */

  .project-title {
    color: var(--text-primary);
    word-wrap: break-word;
    overflow-wrap: break-word;
    hyphens: auto;
    text-align: left;
    width: 100%;
    padding: 0;
    margin: 0;
  }
  @media (min-width: 640px) {
    .project-title {
      text-align: center;
      width: auto;
      padding: 0;
    }
  }
  .project-description {
    color: var(--text-secondary);
    line-height: 1.6;
    text-align: left;
    width: 100%;
    padding: 0;
    margin: 0;
  }
  @media (min-width: 640px) {
    .project-description {
      text-align: center;
      width: auto;
      padding: 0;
    }
  }

  .tech-badge {
    background-color: var(--bg-tertiary);
    color: var(--text-secondary);
    transition: all 0.3s ease;
    font-weight: 500;
  }

  .tech-badge:hover {
    background-color: var(--text-primary);
    color: var(--bg-primary);
    transform: translateY(-2px);
  }

  .btn-source {
    background-color: var(--text-primary);
    color: var(--bg-primary);
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .btn-source:hover {
    opacity: 0.9;
    transform: translateY(-2px);
    box-shadow: 0 8px 15px var(--shadow);
  }

  .btn-preview {
    background-color: var(--bg-primary);
    color: var(--text-primary);
    border: 2px solid var(--text-primary);
    font-weight: 600;
    transition: all 0.3s ease;
  }


  .btn-preview:hover {
    background-color: var(--text-primary);
    color: var(--bg-primary);
    transform: translateY(-2px);
    box-shadow: 0 8px 15px var(--shadow);
  }


  /* Mobile-specific button and badge optimizations */
  @media (max-width: 640px) {
    .btn-source,
    .btn-preview {
      min-height: 44px;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .tech-badge {
      min-height: 32px;
      display: flex;
      align-items: center;
      justify-content: center;
    }
  }

  /* Improve text rendering on mobile */
  @media (max-width: 768px) {
    .project-title,
    .project-description {
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }
  }

  /* Reduce motion for users who prefer it */
  @media (prefers-reduced-motion: reduce) {
    .project-card,
    .tech-badge,
    .btn-source,
    .btn-preview,

    
    .project-card:hover {
      transform: none !important;
    }
  }

  /* High contrast mode support */
  @media (prefers-contrast: high) {
    .project-card {
      border-width: 1px;
    }
    
    .btn-source,
    .btn-preview {
      border-width: 2px;
    }
  }
</style>